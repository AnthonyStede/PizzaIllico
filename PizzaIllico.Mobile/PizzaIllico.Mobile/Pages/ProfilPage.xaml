<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:vm="clr-namespace:PizzaIllico.Mobile.ViewModels"
             x:Class="PizzaIllico.Mobile.Pages.ProfilPage"
             >


    <ContentPage.BindingContext>
        <vm:ProfilViewModel/>
    </ContentPage.BindingContext>

    <StackLayout Orientation="Vertical"
                 VerticalOptions="Center"
                 Spacing="20"
                 Padding="20">

        <Label Text="{Binding Email}" TextColor="Red"/>

        <Label Text="{Binding UserFirstName}" TextColor="Red"/>

        <Label Text="{Binding UserLastName}" TextColor="Red"/>

        <Label Text="{Binding PhoneNumber}" TextColor="Red"/>
        
    </StackLayout>
</ContentPage>